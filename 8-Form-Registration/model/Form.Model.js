const Form = class From {

  constructor(username, email, password, passwordConfirm, birthDate) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.passwordConfirm = passwordConfirm;
    this.birthDate = birthDate;
  }
}
export default Form